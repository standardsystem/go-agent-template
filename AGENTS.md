# go-agent-template — エージェント運用ルール

本ファイルは、このリポジトリで作業する **すべての AI エージェント**（Claude Code /
Codex CLI / Gemini CLI / Cursor 等）が従う共通ルールの正典。エージェント固有の設定は
各ツールの設定ファイルに置き、本ファイルを参照する（Claude Code は
[CLAUDE.md](CLAUDE.md) が `@AGENTS.md` で本ファイルを取り込む。Codex は
[CODEX.md](CODEX.md) を案内として読む）。

> **テンプレート利用者へ**: 「【要記入】」を検索し、プロジェクト固有の内容に置き換える。
> 置き換え終えたらこの注記を消す。テンプレートの使い方は [README.md](README.md)。

## Codex の読み始め

Codex は作業開始時に [CODEX.md](CODEX.md) と [.agent/rules.md](.agent/rules.md) を読むこと。
[CODEX.md](CODEX.md) に、情報の参照順・既存手順の読み替え・スキル対応表を集約している。
Codex が発見する入口は [.agents/skills/](.agents/skills/) に置き、手順本体は
[.agent/skills/](.agent/skills/) を参照する。リンク先は自動展開されたとみなさず、
タスクに必要な本体ファイルを実際に開いて適用すること。

## BASE RULES

- アーティファクト、コミットコメント、タスクの内容、コードコメント、その他すべての
  ドキュメントは日本語を使うこと
- 開発環境は **Windows** とする。ターミナルコマンドは **PowerShell 7 (pwsh)**（VS Code
  統合ターミナル）を前提とすること。Windows PowerShell 5.1 は前提にしない（導入手順は
  [PowerShell 7 セットアップ](docs/manuals/POWERSHELL7_SETUP.md)）。WSL は前提としない
- ツールのバージョン固定・タスク実行・環境構築には `mise` を利用すること
  （[.mise.toml](.mise.toml)）。複数の操作はできるだけ `mise` のタスクにまとめ、
  OS 固有のシェルハック（`[Console]::OutputEncoding` の都度変更など）に依存しないこと
- 文字コードは UTF-8（BOM なし）、改行は LF を基本とする（正典は
  [.gitattributes](.gitattributes)。Windows 専用スクリプト `.ps1` `.cmd` `.bat` だけ CRLF）
- デバッグ用のテキストを作成する際には拡張子を `.log` とすること（`.gitignore` 対象）
- エージェントがスクリプトやツールを作成する必要がある場合は、JavaScript や Python では
  なく **Go** を使用すること（`cmd/cli/<ツール名>/` に置く。手順は
  [scaffold_new_tool](.agent/skills/scaffold_new_tool/SKILL.md)）
- テンポラリのファイル出力が必要な場合は **`./temp` フォルダだけ**を使用すること。
  一時ディレクトリはここ 1 つで、`./tmp` や OS の一時ディレクトリは使わない
  - チケット対応・調査の作業ファイル、添付ダウンロードは
    **`./temp/<YYYYMMDD>_<チケット>_<内容>/`** にまとめる（`data/` `output/` を含む
    命名規約の正典は
    [docs/development/UNTRACKED_DIR_NAMING.md](docs/development/UNTRACKED_DIR_NAMING.md)）
  - 受領データは `./data`、成果物・他チームへの受け渡し物は `./output`（いずれも Git
    管理外）
  - 作業完了時に「昇格（再利用する）」か「削除（一回限り）」を判断すること
- 【要記入】外部データソースへの問い合わせ規約があればここに書く。例: クエリは
  `./temp` に `.sql` ファイルとして置き、結果を `.log` に出力する。インライン実行は
  しない（文字化け回避と再現性のため）

## 作業の進め方（合意ゲート）

[.agent/rules.md](.agent/rules.md) のエージェント行動規範を、着手可否の判断基準として
整理したもの。**「調査は自律的に、変更は合意の上で」** が原則。

### 合意なしで最後まで進めてよいこと

- 調査・読み取り・検索・集計・参照クエリ・レポート作成
- 上記に伴う読み取り専用コマンドの実行（並列実行・チェーンして構わない）
- スコープを勝手に縮小せず最後まで仕上げる。何をどこまで調べたか、何を根拠に何を結論
  づけたかを日本語で報告する

### 着手前に方針を共有し、合意を得ること

次の操作は「取り消しが難しい」「外部に影響する」ものとして扱う:

- ソースコード・設定ファイルの編集、環境構築・依存追加・ツール導入
- `git commit` / `git push` / PR 作成・マージ
- デプロイ、外部データストアへの書き込み（ロード・DDL・DML）
- 課題管理ツール・GitHub への投稿・ステータス変更・アサイン変更
- 個人情報を含みうるファイルの複製・移動

### 合意済みの作業でも守ること

- 合意した**単一の操作だけ**を実行し、未合意の前段処理を推測で補わない。
  例:「push して」と言われてコミットが済んでいなければ、コミットせずに
  「未コミットです」と報告して止まる
- 状態を変えるコマンドは 1 操作ずつ実行し、結果を確認してから次に進む
- **3 分ルール**: 明示依頼以外の作業で 3 分以上進捗が出ないときは中断し、状況を報告して
  指示を仰ぐ（認証情報が無い・環境不備などは独断で深追いしない）
- 個人情報（氏名・連絡先・ID 等）を発見したら直ちに報告し、指示を仰ぐ
- テストコードの作成・修正時は [.agent/rules.md](.agent/rules.md) のテスト設計規範
  （AAA 形式・`t.Logf` による日本語ログ・既存日本語コメントの維持）に従う

## ドメインナレッジ（作業着手前に必読）

【要記入】このプロジェクトの背景・対象システム・前提を示す資料をここに列挙する。
エージェントは推測で結論を出さず、着手前に必ず読む。

- **[docs/knowledge/index.md](docs/knowledge/index.md)** — ドメインナレッジ集の索引
  （索引 → 本体の階層構造。各本体はフロントマターを持つ）
- 【要記入】オンボーディングガイド・設計書・用語集など

## Markdown ファイル作成ガイドライン

Markdown を作成・編集するときは lint（`mise run lint:docs`。設定は
[.markdownlint-cli2.yaml](.markdownlint-cli2.yaml)）に通る形で書くこと:

- **行長**: 80〜100 文字程度を目安に折り返す（日本語向けに MD013 は無効化しているが、
  diff の読みやすさのため長すぎる行は避ける）
- **行末空白**: 残さない（Markdown の 2 スペース改行も使わない）
- **ファイル末尾**: 改行 1 つで終える
- **コードブロック**: 言語を必ず指定する（`text` でもよい）
- **文字コード**: UTF-8（BOM なし）
- 仕上げ前に `mise run lint:docs` を通す

## 内部ライブラリの利用ルール

- **最初の参照先**: `internal/` 配下のライブラリを調べる前に、必ず
  [内部ライブラリ索引](docs/development/INTERNAL_LIBS.md) で API と構造を確認する。
  必要な情報が索引にない場合にソースコードを読む
- **索引の保守**: 内部ライブラリの公開 API（構造体・インターフェース・公開関数）を
  追加・変更・削除した場合は、同索引も更新する

## ドメイン知識の蓄積先

プロジェクトメモリは [docs/knowledge/](docs/knowledge/index.md)（索引 → 本体の階層構造）
を正本とする。新しいドメイン知識はエージェントのローカルメモリから無害化（個人情報・
認証情報・社外秘の除去）のうえ `docs/knowledge/` へ昇格し、`index.md` に 1 行追記する
こと。追加するドキュメントにはフロントマター（最低限 `type`）を付ける。書き方は
[docs/knowledge/README.md](docs/knowledge/README.md) 参照。
