# 環境変数の雛形。コピーして .env を作る (.env は Git 管理外。mise が自動で読み込む)。
#   Copy-Item .env.sample .env
#
# 秘密情報は .env にだけ置き、この雛形にはダミー値か空を書く。

# --- ログ設定 ---
# ログレベル (DEBUG, INFO, WARN, ERROR)。cmd/cli/ 配下のツールが参照する
LOG_LEVEL=INFO
